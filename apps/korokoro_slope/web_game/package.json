{
  "name": "korokoro_slope_web_game",
  "version": "1.0.0",
  "description": "korokoro_slope game core (three.js + cannon-es), bundled into ../assets/web for the Flutter WebView shell",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node generate_model_data.js && esbuild src/main.js --bundle --minify --sourcemap --format=iife --target=es2019 --outfile=dist/game.js && node copy_static.js && node copy_to_flutter_assets.js",
    "watch": "node generate_model_data.js && esbuild src/main.js --bundle --sourcemap --format=iife --target=es2019 --outfile=dist/game.js --watch"
  },
  "dependencies": {
    "cannon-es": "^0.20.0",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
