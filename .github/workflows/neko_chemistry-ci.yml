name: neko_chemistry CI

on:
  push:
    branches: [main]
    paths:
      - "apps/neko_chemistry/**"
      - ".github/workflows/neko_chemistry-ci.yml"
  pull_request:
    paths:
      - "apps/neko_chemistry/**"
      - ".github/workflows/neko_chemistry-ci.yml"

defaults:
  run:
    working-directory: apps/neko_chemistry

jobs:
  analyze-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze
        run: flutter analyze

      - name: Test
        run: flutter test
