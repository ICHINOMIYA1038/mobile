name: etude_generator CI

on:
  push:
    branches: [main]
    paths:
      - "apps/etude_generator/**"
      - ".github/workflows/etude_generator-ci.yml"
  pull_request:
    paths:
      - "apps/etude_generator/**"
      - ".github/workflows/etude_generator-ci.yml"

defaults:
  run:
    working-directory: apps/etude_generator

jobs:
  analyze-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze
        run: flutter analyze

      - name: Test
        run: flutter test
