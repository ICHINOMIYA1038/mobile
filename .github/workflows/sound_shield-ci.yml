name: sound_shield CI

on:
  push:
    branches: [main]
    paths:
      - "apps/sound_shield/**"
      - ".github/workflows/sound_shield-ci.yml"
  pull_request:
    paths:
      - "apps/sound_shield/**"
      - ".github/workflows/sound_shield-ci.yml"

defaults:
  run:
    working-directory: apps/sound_shield

jobs:
  analyze-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze
        run: flutter analyze

      - name: Test
        run: flutter test
